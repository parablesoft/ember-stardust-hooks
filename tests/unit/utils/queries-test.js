import queries from 'dummy/utils/queries';
import { module, test } from 'qunit';

module('Unit | Utility | queries', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = queries();
    assert.ok(result);
  });
});
